extends ../layouts/application.pug

block header
  = t('views.statuses.new.create')

block content
  +formFor()(action=route('createStatus') method='post')
    +input(status, t('views.statuses.index.name'), 'name', errors)(type="text")
    +submit(status)(value=t('views.statuses.new.submit'))
